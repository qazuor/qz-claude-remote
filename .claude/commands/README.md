# Commands

This directory contains custom command definitions for the project.

## Creating Commands

Create `.md` files in this directory to define custom commands. Each command file should include:

1. **Purpose** .. What the command does
2. **Usage** .. How to invoke the command
3. **Process** .. Step-by-step instructions for execution
